@model List<CouponDto>
@{
    ViewData["Title"] = "Coupons";
}
<div class="container border p-4">
    <div class="row text-center">
        <h1>Coupons List</h1>
    </div>
    <a asp-action="Create" class="btn btn-outline-primary">Add New</a>
    <table class="table table-striped">
        <thead>
        <tr>
            <th>Id</th>
            <th>Code</th>
            <th>Discount</th>
            <th>Min Amount</th>
            <th></th>
        </tr>
        </thead>
        <tbody>
        @foreach (var dto in Model)
        {
            <tr>
                <td>@dto.Id</td>
                <td>@dto.Code</td>
                <td>@dto.DiscountAmount.ToString("c")</td>
                <td>@dto.MinAmount.ToString("c")</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@dto.Id" class="btn btn-secondary">Edit</a>
                    <a asp-action="Delete" asp-route-id="@dto.Id" class="btn btn-danger">Delete</a>
                </td>
            </tr>
        }
        </tbody>
    </table>
</div>